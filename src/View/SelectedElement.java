package View;

/**
 * An enumeration that indicates what kind of element is selected so that the corresponding
 * dialog box can be drawn accordingly.
 */
public enum SelectedElement {
		VIEW, 
		PARTY, 
		INVOCATION_MESSAGE, 
		RESULT_MESSAGE;
	
}
